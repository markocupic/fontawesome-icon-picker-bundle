# Go to the project directory first
cd vendor/markocupic/fontawesome-icon-picker-bundle

rd /s /q public
# Update project files (builds the node_modules directory)
#npm update

# Build assets
#npm run build
